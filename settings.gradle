pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}


/* I have commented this because after adding maven in repositories in build.gradle(ALGO) i am getting error
* maven is added to display gifs on splash screen */

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}


rootProject.name = "Algo"
include ':app'
